# 仓库协作说明

## 语言与输出

- 默认使用中文说明问题、方案、验证结果与文档内容。
- 代码注释与新增文档优先使用中文；引用外部术语时可保留必要英文名词。

## 仓库事实

- 当前仓库是一个纯 Python 的机器人 Agent Runtime 原型。
- 默认核心路径可离线运行；ROS2 与 LLM 属于可选扩展，不是每次任务的必验范围。
- 业务代码集中在 `src/robot_agent/`；基础运行与验证入口以 `README.md` 为准。
- 当前仓库未提供版本化的项目级 workflow skill 文件；如后续新增，只记录仓库特有约束，不重复平台通用规则。

## 资料读取与分析

- 默认只读取与当前任务直接相关的文件，不要求每次任务先通读全部文档或完整扫描全仓。
- 涉及跨模块改动、架构调整、接口变更或较高风险任务时，再扩大阅读范围并先做结构化分析。
- 优先使用当前环境可用且能直接说明问题的工具；工具不可用时，可降级为仓库检索、局部阅读和必要命令验证。
- 本地工具配置或验证快照不作为项目共享规范的唯一来源；共享规则以版本化文件为准。

## 开发、验证与整理

- 验证强度应与改动风险匹配，具体按 `README.md` 的“开发与验证建议”执行。
- 不因模型或流程升级而默认扩大验证范围；也不因任务简单而跳过必要验证。
- 默认不要求每次任务都提交 `commit`；仅当用户要求、任务明确包含提交，或达到清晰里程碑时再提交。
- 只整理本轮产生的临时文件，不顺手清理与当前任务无关的内容。

## 完成与确认

- 审查类任务：给出问题位置、影响和调整方案，等待确认后再修改。
- 文档 / 规则类任务：更新完成并做必要 smoke check 后即可完成。
- 代码类任务：完成实现、相应验证，以及必要的测试 / 文档同步后即可完成。
- 在信息足够且风险可控时直接推进；只有继续执行会带来明显返工或风险时，才提出补充问题。
- 默认不要求每次任务都更新 Memory、通读全部文档，或重复确认已明确的范围。
- 删除、重置、推送、权限变更、敏感配置修改等高风险操作，必须先获得明确确认。

## 本地目录说明

- `.claude/`、`.omc/` 等目录默认为本地状态或私有配置。
- 除非后续明确转为版本化资产，否则不将这些目录中的内容视为项目共享规则。
